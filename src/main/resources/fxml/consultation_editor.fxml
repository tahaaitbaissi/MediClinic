<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/8" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.mediclinic.controller.ConsultationEditorController" spacing="10">
    <padding>
        <Insets top="15" right="15" bottom="15" left="15" />
    </padding>
    <children>
        <Label fx:id="titleLabel" text="Consultation médicale" styleClass="title" />
        <GridPane hgap="10" vgap="8">
            <columnConstraints>
                <ColumnConstraints halignment="RIGHT" minWidth="120" />
                <ColumnConstraints hgrow="ALWAYS" />
            </columnConstraints>
            <children>
                <Label text="Patient:" GridPane.rowIndex="0" GridPane.columnIndex="0" />
                <Label fx:id="patientLabel" GridPane.rowIndex="0" GridPane.columnIndex="1" />

                <Label text="Date:" GridPane.rowIndex="1" GridPane.columnIndex="0" />
                <Label fx:id="dateLabel" GridPane.rowIndex="1" GridPane.columnIndex="1" />

                <Label text="Motif:" GridPane.rowIndex="2" GridPane.columnIndex="0" />
                <Label fx:id="motifLabel" GridPane.rowIndex="2" GridPane.columnIndex="1" />
            </children>
        </GridPane>

        <TitledPane text="Observations">
            <content>
                <TextArea fx:id="observationsArea" prefRowCount="4" wrapText="true" />
            </content>
        </TitledPane>

        <TitledPane text="Diagnostic">
            <content>
                <TextArea fx:id="diagnosticArea" prefRowCount="4" wrapText="true" />
            </content>
        </TitledPane>

        <TitledPane text="Prescriptions">
            <content>
                <TextArea fx:id="prescriptionsArea" prefRowCount="4" wrapText="true" />
            </content>
        </TitledPane>

        <Separator />

        <!-- Signature Option -->
        <VBox spacing="8" style="-fx-background-color: #f0f8ff; -fx-border-color: #4682b4; -fx-border-width: 2; -fx-border-radius: 5; -fx-background-radius: 5; -fx-padding: 10;">
            <Label text="Options de signature" style="-fx-font-weight: bold; -fx-font-size: 14px; -fx-text-fill: #2c3e50;" />
            <CheckBox fx:id="includeSignatureCheckBox" text="Inclure signature électronique" selected="true" style="-fx-font-size: 12px;" />
            <Label text="💡 La signature sera automatiquement ajoutée au bas du document PDF" style="-fx-font-size: 11px; -fx-text-fill: #7f8c8d; -fx-font-style: italic;" wrapText="true" />
        </VBox>

        <HBox spacing="10" alignment="CENTER_LEFT">
            <Button fx:id="saveBtn" text="💾 Enregistrer" styleClass="btn-success btn-actions" onAction="#handleSave" style="-fx-font-size: 13px; -fx-padding: 8 20 8 20;" />
            <Button fx:id="exportPdfBtn" text="📄 Exporter PDF" styleClass="btn-primary btn-actions" onAction="#handleExportPdf" style="-fx-font-size: 13px; -fx-padding: 8 20 8 20; -fx-background-color: #3498db; -fx-text-fill: white;" />
            <Button text="❌ Fermer" styleClass="btn-danger btn-actions" onAction="#handleClose" style="-fx-font-size: 13px; -fx-padding: 8 20 8 20;" />
        </HBox>
    </children>
</VBox>
